---
:backends:
  - yaml

:yaml:
<% if @kernel == 'windows' -%>
  :datadir: C:\puppetcode\hieradata
<% else -%>
  :datadir: /etc/puppetlabs/code/hieradata
<% end -%>

:hierarchy:
  - "%{clientcert}"
  - "%{osfamily}"
  - common
